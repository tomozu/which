<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>which</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width">
  </head>
  <body>
  <header>
    <ul>
      <nav class="navbar navbar-default navbar-fixed-top">
        <!--<div class="container-fluid">-->
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#gnavi">
              <span class="sr-only">which</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
    <ul/>        
     <a class="navbar-brand" href="#">which</a>
     </div>
     <div id="gnavi" class="collapse navbar-collapse">
      <ul class="nav bar-nav mr-auto">
      <ul class="global-nav">
  
    <% if logged_in? %>
      <li><%= link_to '投稿一覧', posts_path %></li>
  　　<li><%= link_to 'マイページ', user_path(current_user.id) %></li>
      <li><%= link_to '新規投稿', new_post_path %></li>
      <li><%= link_to 'ログアウト', session_path(current_user.id), method: :delete %></li>
        <% else %>
      <li><%= link_to '新規会員登録', new_user_path %></li>
      <li><%= link_to 'ログイン', new_session_path %></li>
        <% end %>
     </nav>
    </ul>
   <section class="main-header">
    <%= image_tag"portfolio-photo.jpg", class:"image_area" %> 
   </section>

  </header>
  
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>
    
    <%= yield %>
    
  <footer>
    <small>2018 sotugyo-kadai.</small>
  </footer>
  </body>
</html>